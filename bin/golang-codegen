#!/bin/bash

root_dir=$(dirname $0)/..
golang_dir=$root_dir/src/golang
proto_dir=$root_dir/src/proto
proto_file=$proto_dir/device.proto

protoc -I $proto_dir $proto_file --go_out=plugins=grpc:$golang_dir